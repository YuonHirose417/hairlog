/**
 * 開発中だけ有効になるフラグ。
 *
 * すべて `__DEV__ &&` を頭に付けること。リリースビルドでは `__DEV__` が false に
 * なるため、値を true のまま置き忘れても本番の挙動には影響しない。
 */

/**
 * 無料枠（記録5件）の判定を飛ばす。
 *
 * 未購入・5件以上のとき ＋ ボタンが常に購入画面へ飛んでしまい、記録追加の
 * 開発が進められなくなるため。課金導線を確認したいときは false に戻す。
 */
export const DEV_SKIP_PAYWALL = __DEV__ && true;
