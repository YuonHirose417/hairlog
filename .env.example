# RevenueCat iOS 公開キー（Apple 用の public SDK key）
# RevenueCat ダッシュボード > Project settings > API keys から取得し、
# .env にコピーして値を入れる。.env はコミットしないこと。
# 未設定でもアプリは起動し、課金機能だけが無効になる。
EXPO_PUBLIC_REVENUECAT_IOS_KEY=
